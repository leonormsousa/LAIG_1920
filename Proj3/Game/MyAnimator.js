/**
 * MyAnimator
 */
class MyAnimator extends CGFobject {
	constructor(scene, gameMove) {
        super(scene);
        this.gameMove = gameMove; 
        this.over=false;
    }

    update(t){
    }

    display(){
    }
}